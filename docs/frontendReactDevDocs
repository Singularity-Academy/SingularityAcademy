
## Testing : current using nohup
使用 nohup、tmux、screen 或 pm2 来让 React 项目 在后台持续运行，同时让你还能使用终端进行其他操作。

方法 1：使用 nohup（简单易用）

nohup npm start -- --host 0.0.0.0 > output.log 2>&1 &

	•	nohup 让进程不受终端关闭影响
	•	& 让进程在后台运行
	•	> output.log 2>&1 让日志输出到 output.log

查看日志

tail -f output.log

停止后台进程

ps aux | grep npm  # 找到 `npm start` 的 PID
kill -9 <PID>  # 替换 <PID> 为实际进程号

方法 2：使用 tmux（推荐，支持多会话管理）

tmux 可以让你在后台运行进程，并随时恢复会话。

安装 tmux

sudo apt install tmux -y

创建新会话

tmux new -s react-app

然后在会话里运行：

npm start -- --host 0.0.0.0

退出但让进程继续运行

Ctrl + B, 然后按 D

恢复会话

tmux attach -t react-app

终止会话

tmux kill-session -t react-app

方法 3：使用 screen（类似 tmux）

如果你不想安装 tmux，可以用 screen：

安装 screen

sudo apt install screen -y

创建 screen 会话

screen -S react-app

然后运行：

npm start -- --host 0.0.0.0

退出但保持运行

Ctrl + A, 然后按 D

恢复 screen 会话

screen -r react-app

终止会话

exit  # 在 `screen` 会话内输入 exit

方法 4：使用 pm2（专门用于 Node.js 应用）

pm2 是一个专门管理 Node.js 进程的工具，可以保证 React 项目一直运行，即使服务器重启也能自动恢复。

安装 pm2

npm install -g pm2

启动 React 项目

pm2 start "npm start -- --host 0.0.0.0" --name react-app

查看进程

pm2 list

重启项目

pm2 restart react-app

停止项目

pm2 stop react-app

删除项目

pm2 delete react-app

让 pm2 在服务器重启时自动启动

pm2 startup
pm2 save

总结

方法	适用场景	命令
nohup	最简单的方式，适合临时运行	nohup npm start -- --host 0.0.0.0 > output.log 2>&1 &
tmux	推荐，支持多会话恢复	tmux new -s react-app
screen	类似 tmux，无须安装额外软件	screen -S react-app
pm2	最专业，适合生产环境	pm2 start "npm start -- --host 0.0.0.0" --name react-app

最佳推荐
	•	如果只是短期后台运行，nohup 最简单
	•	如果想管理多个后台进程，tmux 更灵活
	•	如果是生产环境，pm2 最稳定可靠
