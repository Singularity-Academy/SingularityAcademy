services:
  backend-mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - backend_mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    restart: unless-stopped
    networks:
      - app-network

  ai-mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${AI_DB_PASSWORD:-${DB_PASSWORD}}
      MYSQL_DATABASE: ${AI_DB_NAME:-singularity_ai}
      MYSQL_USER: ${AI_DB_USER:-ai_user}
      MYSQL_PASSWORD: ${AI_DB_PASSWORD:-${DB_PASSWORD}}
    volumes:
      - ai_mysql_data:/var/lib/mysql
    ports:
      - "3307:3306"
    restart: unless-stopped
    networks:
      - app-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - DB_HOST=backend-mysql
      - DB_PORT=3306
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - backend-mysql
    restart: unless-stopped
    networks:
      - app-network

  ai-engine:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DB_HOST=ai-mysql
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=${AI_DB_PASSWORD:-${DB_PASSWORD}}
      - DB_NAME=${AI_DB_NAME:-singularity_ai}
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./ai_engine/config:/app/ai_engine/config
    depends_on:
      - ai-mysql
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - app-network

  caddy:
    image: caddy:2-alpine
    ports:
      - "1298:1298"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - backend
      - ai-engine
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  backend_mysql_data:
  ai_mysql_data:
  caddy_data:
  caddy_config: